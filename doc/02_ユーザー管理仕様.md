ご指摘の点、大変合理的な権限分離です。

`admin` を純粋な「システム・アカウント管理」に専念させ、業務運用を「一次採点スタッフ」と「選考・集計管理者」に分けることで、誤操作のリスクを減らし責任範囲を明確にできます。

ご要望を反映し、**ロール構造を全面的に整理した修正版仕様書**を作成いたしました。

---

# 認証・ユーザー管理機能 システム仕様書（修正版）

---

## 1. 概要・設計方針

### 1.1 目的

本仕様書は、小中学生向けプログラミングコンテストシステムにおける「ユーザー認証」「招待リンクによる権限自動付与」「管理者・運営用ユーザー管理」に関する要件およびデータベース設計を定義するものである。

### 1.2 基本方針

* **役割に応じた権限の完全分離**: `admin`（システム管理者）はユーザー管理およびシステム保守のみを担当し、コンテストの選考・集計作業には直接関与しない。業務運用は `staff_primary`（一次採点）、`staff_manager`（集計管理）、`judge`（審査員）に分離する。
* **アカウント統合（Same Email Linking）**: 同一のメールアドレスであれば、Google OAuth（SSO）およびメールアドレス＋パスワードのどちらからサインインしても同一人物のアカウント（同一レコード）として統合・処理する。
* **フレキシブルな招待機能**: 管理者は招待先（各ロール）に応じたワンタイムトークン付きの招待URLを発行でき、利用上限回数（1回のみ／指定回数／無制限）および有効期限を設定できる。

---

## 2. ユーザーロールおよび権限定義

システム内で定義する主要なユーザーロールとアクセス権限は下表の通りです。

| ロール名 | ロールID (`user_role`) | 対象ユーザー | システム内のアクセス権限 |
| --- | --- | --- | --- |
| **応募者** | `applicant` | 児童・生徒・保護者 | 新規登録時の初期ロール。作品の応募・修正、マイページの閲覧 |
| **一次採点スタッフ** | `staff_primary` | 事務局スタッフ | 一次審査（応募作品の動作確認・不備チェック・スクリーニング・一次採点）のみ |
| **集計管理スタッフ** | `staff_manager` | 運営事務局コアメンバー | 一次審査（必要に応じて採点可）、**審査進捗モニタリング、リアルタイム集計、上位選抜（20枠決定）、ランキング出力、不備データの修正・データメンテ** |
| **審査員** | `judge` | 審査員 | 二次審査（上位作品の詳細採点・ルーブリック評価・各種コメントの入力） |
| **システム管理者** | `admin` | システム運用担当 | **ユーザー管理（一覧・権限変更・アカウント有効化/無効化）、各種招待リンクの発行・管理のみ**（※選考や集計操作は不可） |

---

## 3. 機能要件

### 3.1 認証・アカウント管理機能（全ユーザー共通）

1. **マルチ認証プロバイダ対応**:
* Google ID連携認証（Google OAuth）によるワンタップログイン / 新規登録。
* メールアドレス＋パスワードによる新規登録 / ログイン。


2. **アカウント自動統合（同一メールアドレス）**:
* 事前にメールアドレス検証（確認メールの送付・認証リンクのクリック）を必須化。
* 検証済みのメールアドレスが一致する場合、Googleログインとメール＋パスワードログインを同一の `Users` レコードに紐付ける。



### 3.2 招待・ロール付与機能（管理者 ➔ 運営・審査員）

1. **招待リンク発行機能（管理者用）**:
* 管理者が付与したい権限（`staff_primary` / `staff_manager` / `judge`）を選択してリンクを発行。
* **利用回数上限設定**: 「1回のみ（単発）」「指定回数（例: 10回）」「無制限」から選択可能。
* **有効期限設定**: デフォルト24時間（任意変更可）。


2. **招待リンク経由の登録フロー**:
* 対象者が招待URL（例: `[https://example.com/invite?token=xxx](https://example.com/invite?token=xxx)`）へアクセス。
* 有効性チェック（有効期限内かつ `use_count < max_uses` であること）を実施。
* 対象者がGoogleログインまたはメールアドレス登録を完了した時点で、指定されたロールを自動付与。
* 利用回数 `use_count` を +1 インクリメントし、利用履歴（`invite_usages`）を記録。



### 3.3 管理者（Admin）向けユーザー管理機能

1. **ユーザー一覧・検索・フィルタ**:
* 登録ユーザー全件のリスト表示。
* 氏名・メールアドレス・学校名によるキーワード検索。
* ロール別（5つのロール）およびステータス別のフィルタリング。


2. **ロール直接変更・アカウント制御**:
* 登録済みユーザーのロールを手動変更する機能（例: アルバイトスタッフから `staff_manager` への昇格など）。
* 不正利用防止のためのアカウント無効化（`is_active = false`）機能。


3. **招待管理・モニタリング**:
* 発行済み招待リンクの一覧表示（残り利用可能回数、有効期限、ステータス表示）。
* リンクの手動無効化（キャンセル）および再発行機能。



---

## 4. データベース設計（SQL DDL）

`user_role` に `staff_primary` と `staff_manager` を追加・変更した修正版スクリプトです。

```sql
-- 1. ユーザーロール定義 (ENUM) ※更新
CREATE TYPE user_role AS ENUM (
    'applicant',      -- 応募者
    'staff_primary',  -- 一次採点スタッフ
    'staff_manager',  -- 集計管理スタッフ
    'judge',          -- 審査員
    'admin'           -- システム管理者
);

-- 2. 招待ステータス定義 (ENUM)
CREATE TYPE invite_status AS ENUM ('active', 'exhausted', 'expired', 'cancelled');

-- 3. ユーザーテーブル (users)
CREATE TABLE users (
    user_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email VARCHAR(255) NOT NULL UNIQUE,          -- アカウント統合のキーとなる一意アドレス
    auth_provider VARCHAR(50) NOT NULL DEFAULT 'email', -- 'google', 'email', 'multiple'
    name VARCHAR(100) NOT NULL,
    name_kana VARCHAR(100),
    school_name VARCHAR(100),
    grade VARCHAR(20),
    guardian_name VARCHAR(100),
    guardian_email VARCHAR(255),
    guardian_phone VARCHAR(50),
    role user_role NOT NULL DEFAULT 'applicant', -- 単一ロール管理
    is_active BOOLEAN NOT NULL DEFAULT true,     -- アカウント有効化フラグ（メール検証完了済）
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

-- 4. 招待管理テーブル (invites)
CREATE TABLE invites (
    invite_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    token VARCHAR(255) NOT NULL UNIQUE,           -- ワンタイム/マルチユース トークン
    target_role user_role NOT NULL,             -- 付与する権限 ('staff_primary', 'staff_manager', 'judge')
    max_uses INT NOT NULL DEFAULT 1,            -- 利用上限回数 (1: 単発, N: 複数回利用可能)
    use_count INT NOT NULL DEFAULT 0,           -- 現在の利用達成回数
    status invite_status NOT NULL DEFAULT 'active',
    expires_at TIMESTAMP WITH TIME ZONE NOT NULL, -- 有効期限
    created_by_user_id UUID NOT NULL REFERENCES users(user_id), -- 発行した管理者ID
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

-- 5. 招待利用履歴テーブル (invite_usages)
CREATE TABLE invite_usages (
    usage_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    invite_id UUID NOT NULL REFERENCES invites(invite_id) ON DELETE CASCADE,
    used_by_user_id UUID NOT NULL REFERENCES users(user_id), -- 登録・権限昇格したユーザーID
    used_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

-- 検索・パフォーマンス向上のためのインデックス設定
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_users_role ON users(role);
CREATE INDEX idx_invites_token ON invites(token);
CREATE INDEX idx_invites_status ON invites(status);
CREATE INDEX idx_invite_usages_invite ON invite_usages(invite_id);

```

---

これですべての要件・権限分離が完璧に固まりました。

この構成を元に、次のステップとして「1. 管理者画面の画面UI設計（ワイヤーフレーム）」**または**「2. Next.js / Supabase等による認証・招待コードの実装」のどちらに進みますか？